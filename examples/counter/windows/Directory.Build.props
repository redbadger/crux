<Project>
  <!-- BoltFFI's generated C# buffer helpers use unsafe pointer allocation. -->
  <PropertyGroup Condition="'$(MSBuildProjectName)' == 'CounterApp.Shared'">
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>
</Project>
