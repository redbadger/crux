# Development

Install the Dioxus CLI globally:

```bash
cargo install --force dioxus-cli@0.7.2
```

Run the following command in the root of the project to start the Dioxus dev server:

```bash
dx serve --web
```

- Open the browser to http://localhost:8080
