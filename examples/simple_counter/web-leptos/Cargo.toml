[package]
name = "web-leptos"
version = "0.1.0"
edition = "2021"

[dependencies]
leptos = { version = "0.7.4", features = ["csr"] }
shared = { path = "../shared" }

[lints.clippy]
empty_docs = "allow"
