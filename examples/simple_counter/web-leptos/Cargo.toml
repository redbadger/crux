[package]
name = "web-leptos"
version = "0.1.0"
edition = "2021"

[dependencies]
leptos = { version = "0.6.12", features = ["csr"] }
shared = { path = "../shared" }

[lints.clippy]
empty_docs = "allow"
